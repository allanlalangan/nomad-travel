// styles and ui
import styles from './Map.module.css';

const Map = () => {
  return (
    <section className={`${styles['map-section']}`}>
      <h2>Map</h2>
    </section>
  );
};

export default Map;
