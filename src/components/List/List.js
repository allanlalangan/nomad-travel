// styles and ui
import styles from './List.module.css';

const List = () => {
  return (
    <section className={`${styles['list-section']}`}>
      <h2>List</h2>
    </section>
  );
};

export default List;
