const style = {
  appBar: {
    width: '100%',
    height: (theme) => `${theme.layout.appBarHeight}vh`,
  },
  toolBar: {
    height: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
};

export default style;
