const style = {
  fieldContainer: {
    backgroundColor: (theme) => theme.palette.background.default,
    // margin: '',
    padding: '1rem 0',
  },
  filterField: {
    width: '100%',
  },

  checkboxLiItem: {
    width: '100%',
    height: '100%',
    padding: 0,
  },
};

export default style;
