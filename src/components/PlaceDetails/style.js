const style = {
  container: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: (theme) => theme.palette.background.default,
    width: '100%',
    height: '100%',
    padding: '1.5rem',
    borderRadius: '1rem',

    position: 'absolute',
  },
};

export default style;
